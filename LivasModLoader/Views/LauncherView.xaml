<UserControl x:Class="LivasModLoader.Views.LauncherView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <StackPanel>
        <TextBlock Text="Запуск игры" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,16" />
        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="Платформа:" VerticalAlignment="Center" Margin="0,0,12,0" />
            <ComboBox Width="200" SelectedItem="{Binding SelectedPlatform}">
                <ComboBoxItem Content="Steam" />
                <ComboBoxItem Content="Epic" />
            </ComboBox>
        </StackPanel>
        <ItemsControl ItemsSource="{Binding Profiles}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="3" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource CosmicCardStyle}" Margin="0,0,12,0" Background="#1B2B4A">
                        <StackPanel>
                            <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="SemiBold" />
                            <TextBlock Text="Профиль запуска" Foreground="{StaticResource CosmicMutedBrush}" Margin="0,4,0,16" />
                            <Button Content="Запустить" Style="{StaticResource PrimaryButtonStyle}"
                                    Command="{Binding DataContext.LaunchCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
